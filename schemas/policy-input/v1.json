{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-delivery-spec.dev/schemas/policy-input/v1.json",
  "title": "ODS Policy Input v1",
  "description": "The structured object ODS feeds to OPA Rego policy evaluation at the check stage. All fields are set by the ODS CLI pipeline (detect → analyze → score) and are available as `input.*` inside your Rego policy.",
  "type": "object",
  "required": ["ai_generated", "ai_confidence"],
  "properties": {
    "ai_generated": {
      "type": "boolean",
      "description": "Whether AI code was detected in the diff."
    },
    "ai_confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Aggregate AI detection confidence (0.0–1.0). 0 means no signal; 1.0 means multiple high-confidence signals."
    },
    "detection_sources": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["commit-trailer", "git-ai-notes", "pr-body", "branch-name", "diff-heuristics"]
      },
      "description": "Which detection signals fired. Lets policies separate disclosed AI use (author attribution: commit-trailer, git-ai-notes, pr-body) from merely suspected AI use (branch-name, diff-heuristics only), making author-disclosure norms (SFC guidance, kernel docs) checkable at the gate."
    },
    "technical_debt_delta": {
      "type": "number",
      "description": "Weighted technical-debt impact of the PR. Higher positive = more debt added. Negative values indicate debt reduction."
    },
    "test_coverage": {
      "type": "number",
      "minimum": -1,
      "maximum": 1,
      "description": "Line coverage rate (0.0–1.0). -1 means not measured (no coverage file found). Policies should guard with `input.test_coverage >= 0` before comparing."
    },
    "test_coverage_source": {
      "type": "string",
      "enum": ["go", "lcov", "cobertura", "nyc", "estimated", "unknown"],
      "description": "How test_coverage was obtained. 'unknown' means it was not measured."
    },
    "patch_coverage": {
      "type": "number",
      "minimum": -1,
      "maximum": 1,
      "description": "Coverage of the diff's ADDED lines only (0.0–1.0): the fraction of newly-added, executable lines that the coverage report marks as covered. Stronger than test_coverage — it asks whether *this change's* new code is tested, not the whole project. -1 means not measured (no per-line coverage report, or no changed source file appears in it; aggregate-only formats such as NYC's coverage-summary.json cannot produce it). Policies MUST guard with `input.patch_coverage >= 0` before comparing."
    },
    "branch": {
      "type": "string",
      "description": "The PR head branch name (e.g. claude/add-auth, feature/oauth)."
    },
    "changed_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Paths of all files changed in the PR (all types, not just code files)."
    },
    "issues": {
      "type": "array",
      "description": "Quality issues found by ods analyze. Each item represents one finding from the heuristic analyzer or an ingested SARIF report.",
      "items": { "$ref": "#/$defs/Issue" }
    },
    "ai_files": {
      "type": "array",
      "description": "Files detected as containing AI-generated code by ods detect.",
      "items": { "$ref": "#/$defs/AIFile" }
    },
    "ai_reviews": {
      "type": "array",
      "description": "AI code-reviewer verdicts supplied via `ods check --ai-review` (see schemas/review-verdict/v1.json). Kept separate from `issues` on purpose: deterministic findings may deny, probabilistic reviewer opinions default to routing attention only. A policy must reference this section explicitly to let LLM findings block.",
      "items": { "$ref": "#/$defs/AIReview" }
    },
    "merge_confidence": {
      "$ref": "#/$defs/MergeConfidence",
      "description": "Deterministic, diff-scoped facts about the change — is it tested, how is it shaped, does it touch sensitive paths. Facts, not opinions: advisory by default (they route review attention), and a policy may opt in to deny on them. Attribution (ai_generated) is used to raise the bar for AI-authored changes, never these signals to detect AI."
    }
  },
  "$defs": {
    "Issue": {
      "type": "object",
      "required": ["rule", "severity", "file", "line"],
      "properties": {
        "rule": {
          "type": "string",
          "description": "Rule identifier. Built-in rules are prefixed ai- (e.g. ai-unsafe-deserialization). SARIF rules use the tool's own id."
        },
        "severity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low", "info"],
          "description": "Issue severity."
        },
        "file": {
          "type": "string",
          "description": "Repository-relative file path."
        },
        "line": {
          "type": "integer",
          "minimum": 0,
          "description": "Line number (0 when the location is not known)."
        },
        "message": {
          "type": "string",
          "description": "Human-readable description of the issue."
        }
      }
    },
    "AIFile": {
      "type": "object",
      "required": ["path"],
      "properties": {
        "path": {
          "type": "string",
          "description": "Repository-relative file path."
        },
        "ai_lines": {
          "type": "integer",
          "minimum": 0,
          "description": "Estimated number of AI-generated lines in the file."
        },
        "total_lines": {
          "type": "integer",
          "minimum": 0,
          "description": "Total lines added in this file."
        },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "AI detection confidence for this file (0.0–1.0)."
        }
      }
    },
    "AIReview": {
      "type": "object",
      "required": ["tool", "verdict"],
      "properties": {
        "tool": {
          "type": "string",
          "description": "The reviewing tool (e.g. claude-code, coderabbit)."
        },
        "model": {
          "type": "string",
          "description": "Model identifier used for the review, when known."
        },
        "verdict": {
          "type": "string",
          "enum": ["approve", "request_changes", "comment"],
          "description": "The reviewer's overall conclusion. Only verdicts matching the evaluated HEAD enter the input — stale verdicts are skipped upstream."
        },
        "findings": {
          "type": "array",
          "description": "Semantic findings from the reviewer.",
          "items": { "$ref": "#/$defs/ReviewFinding" }
        }
      }
    },
    "ReviewFinding": {
      "type": "object",
      "required": ["message"],
      "properties": {
        "file": { "type": "string" },
        "line": { "type": "integer", "minimum": 0 },
        "severity": {
          "type": "string",
          "enum": ["info", "low", "medium", "high", "critical"]
        },
        "category": {
          "type": "string",
          "description": "Free-form finding class, e.g. correctness, design, security."
        },
        "message": {
          "type": "string",
          "description": "Human-readable description of the finding."
        }
      }
    },
    "MergeConfidence": {
      "type": "object",
      "description": "Deterministic diff-scoped signals. All fields are facts computed from the diff alone.",
      "properties": {
        "files_changed": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of files touched (all types)."
        },
        "source_files_changed": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of non-test code files with added lines."
        },
        "test_files_changed": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of test files with added lines."
        },
        "net_added_lines": {
          "type": "integer",
          "minimum": 0,
          "description": "Total added code lines across changed code files (for wide-but-shallow / diff-shape checks)."
        },
        "tests_touched": {
          "type": "boolean",
          "description": "Whether any test file was added or modified."
        },
        "added_source_without_tests": {
          "type": "boolean",
          "description": "Source code was added but no test file was added or updated — the most-cited AI-PR smell. A policy SHOULD warn/route on this; it MAY deny only when the team opts in."
        },
        "risky_paths": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Changed files on sensitive paths — CI config (.github/workflows/), dependency manifests/lockfiles, and auth/crypto/security paths."
        }
      }
    }
  }
}
